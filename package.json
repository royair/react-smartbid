{
  "name": "react-smart-bid",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.25.3",
    "classnames": "^2.2.6",
    "gh-pages": "^2.1.1",
    "module-alias": "^2.2.2",
    "node-sass": "^4.13.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "reset-css": "^5.0.1"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://royair.github.io/react-smartbid/",
  "devDependencies": {
    "react-app-rewire-aliases": "^0.2.0",
    "react-app-rewired": "^2.1.5"
  }
}
